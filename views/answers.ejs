<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>MY APP</title>
</head>
<body>
	<p>May Node and Express be with you.</p>

	<table border="1" style="display: inline-block;">
		<tbody>
			<tr>
				<th colspan="3">Income</th>
			</tr>

			<% for(var i=0; i<quotes.length; i++) { %>
				<% if (quotes[i].name == 'income') { %>
					<tr>
						<td><%= quotes[i].description %></span>
						<td><%= quotes[i].amount %></span>
						<td><%= quotes[i].notes %></span>
					</tr>
				<% } %>

			<% } %>

		</tbody>
	</table>

	<table border="1" style="display: inline-block;">
		<tbody>
			<tr>
				<th colspan="3">Cash Flow</th>
			</tr>

			<% for(var i=0; i<quotes.length; i++) { %>
				<% if (quotes[i].name == 'cashflow') { %>
					<tr>
						<td><%= quotes[i].description %></span>
						<td><%= quotes[i].amount %></span>
						<td><%= quotes[i].notes %></span>
					</tr>
				<% } %>
			<% } %>
		</tbody>
	</table>

	<table border="1" style="display: inline-block;">
		<tbody>
			<tr>
				<th colspan="3">Credit Flow</th>
			</tr>

			<% for(var i=0; i<quotes.length; i++) { %>
				<% if (quotes[i].name == 'creditflow') { %>
					<tr>
						<td><%= quotes[i].description %></span>
						<td><%= quotes[i].amount %></span>
						<td><%= quotes[i].notes %></span>
					</tr>
				<% } %>
			<% } %>
		</tbody>
	</table>

	<div style="display: block;">
		<a href="/">Add Spending</a>
	</div>
</body>
</html>
